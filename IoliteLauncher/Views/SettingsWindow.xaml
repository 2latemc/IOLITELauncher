<Window x:Class="IoliteLauncher.Views.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:IoliteLauncher.Views"
        mc:Ignorable="d"
        Title="Settings" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="Auto" MinHeight="50"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
        </Grid.RowDefinitions>
        <ListView>
            <Label>Engine Path</Label>
            <TextBox x:Name="EngineBox">C:/User/IOLITE</TextBox>
        </ListView>
        <ListView Grid.Row="1">
            <Button Click="RemoveElementButton">Remove Element</Button>
            <Button Click="AddProjectPathElementButton">Add Element</Button>
            <TextBox x:Name="ProjectPathBox">Path to add</TextBox>
            <ListView x:Name="ProjectPathList" FlowDirection="RightToLeft" ItemsSource="{Binding ProjectPathEntries}">
            </ListView>
        </ListView>
        <Button Click="Submit" Grid.Row="2">Apply</Button>
    </Grid>
</Window>
